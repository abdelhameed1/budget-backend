{
  "kind": "collectionType",
  "collectionName": "cashflows",
  "info": {
    "singularName": "cashflow",
    "pluralName": "cashflows",
    "displayName": "Cash Flow",
    "description": "All cash flow transactions"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "transactionDate": {
      "type": "date",
      "required": true
    },
    "type": {
      "type": "enumeration",
      "enum": ["revenue", "expense"],
      "required": true
    },
    "category": {
      "type": "enumeration",
      "enum": [
        "sales",
        "material_purchase",
        "labor_payment",
        "overhead_fixed",
        "overhead_variable",
        "zakat",
        "sadaqat",
        "other"
      ],
      "required": true
    },
    "description": {
      "type": "string",
      "required": true
    },
    "amount": {
      "type": "decimal",
      "required": true,
      "min": 0
    },
    "paymentMethod": {
      "type": "enumeration",
      "enum": ["cash", "bank_transfer", "credit", "other"],
      "default": "cash"
    },
    "reference": {
      "type": "string"
    },
    "supplier": {
      "type": "string"
    },
    "customer": {
      "type": "string"
    },
    "invoiceNumber": {
      "type": "string"
    },
    "isPaid": {
      "type": "boolean",
      "default": true
    },
    "dueDate": {
      "type": "date"
    },
    "notes": {
      "type": "text"
    }
  }
}
