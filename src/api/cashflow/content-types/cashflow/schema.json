{
  "kind": "collectionType",
  "collectionName": "cashflows",
  "info": {
    "singularName": "cashflow",
    "pluralName": "cashflows",
    "displayName": "Cash Flow",
    "description": "Owner capital investments and business expense tracking"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "transactionDate": {
      "type": "date",
      "required": true
    },
    "type": {
      "type": "enumeration",
      "enum": ["owner_investment", "expense"],
      "required": true
    },
    "category": {
      "type": "enumeration",
      "enum": [
        "owner_investment",
        "material_purchase",
        "labor_payment",
        "overhead_fixed",
        "overhead_variable",
        "other"
      ],
      "required": true
    },
    "description": {
      "type": "string",
      "required": true
    },
    "amount": {
      "type": "decimal",
      "required": true,
      "min": 0
    },
    "paymentMethod": {
      "type": "enumeration",
      "enum": ["cash", "bank_transfer", "credit", "other"],
      "default": "cash"
    },
    "reference": {
      "type": "string"
    },
    "owner": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::owner.owner",
      "required": false
    },
    "supplier": {
      "type": "string"
    },
    "invoiceNumber": {
      "type": "string"
    },
    "isPaid": {
      "type": "boolean",
      "default": true
    },
    "dueDate": {
      "type": "date"
    },
    "notes": {
      "type": "text"
    }
  }
}
